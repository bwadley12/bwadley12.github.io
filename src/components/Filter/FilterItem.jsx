import "./FilterItem.css"
import Button from "../Button/Button"

export default function FilterItem({filterValue}) {

    return (
        <div className="filteritem">
            <Button />
        </div>
    )
}